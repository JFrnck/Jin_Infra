apiVersion: v1
kind: Service
metadata:
  name: jin-web
  namespace: jin
  labels:
    app.kubernetes.io/name: jin-web
spec:
  selector:
    app.kubernetes.io/name: jin-web
  ports:
    - name: http
      port: 80
      targetPort: http
