# TLS con el wildcard de jeanfranck.com emitido por cert-manager en este
# namespace. Cloudflare Access protege este hostname además del login
# de Grafana (se configura en Zero Trust — ver runbook, paso 7).
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  namespace: observability
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - grafana.jeanfranck.com
      secretName: wildcard-jeanfranck-com-tls
  rules:
    - host: grafana.jeanfranck.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: grafana
                port:
                  name: http
